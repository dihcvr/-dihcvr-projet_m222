{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>NomDuClient</th>
                <td>{{ client.nomDuClient }}</td>
            </tr>
            <tr>
                <th>PrenomDuClient</th>
                <td>{{ client.prenomDuClient }}</td>
            </tr>
            <tr>
                <th>Telcli</th>
                <td>{{ client.telcli }}</td>
            </tr>
            <tr>
                <th>Adrcli</th>
                <td>{{ client.adrcli }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_index') }}">back to list</a>

    <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
