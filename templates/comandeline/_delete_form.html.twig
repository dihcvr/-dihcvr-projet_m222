<form method="post" action="{{ path('comandeline_delete', {'id': comandeline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comandeline.id) }}">
    <button class="btn">Delete</button>
</form>
