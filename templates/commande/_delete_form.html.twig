<form method="post" action="{{ path('commande_delete', {'idcde': commande.idcde}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.idcde) }}">
    <button class="btn">Delete</button>
</form>
