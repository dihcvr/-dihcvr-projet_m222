<form method="post" action="{{ path('livraison_delete', {'idLiv': livraison.idLiv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison.idLiv) }}">
    <button class="btn">Delete</button>
</form>
